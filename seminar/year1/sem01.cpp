#include <iostream>

using namespace std;

int main() {

    // --- Ex 1 ---

    // int num;
    // cin >> num;

    // cout << (num % 2 == 0);

    // --- Ex 2 ---

    // int num;
    // cin >> num;

    // int result = (num % 10 * 100) + 10 * (num / 10 % 10) + (num / 100 % 10) + 1;

    // if (num % 10 == 0)
    // {
    //     cout << "0" << result;
    // }
    // else
    // {
    //     cout << result;
    // }

    // --- Ex 3 ---

    // char symbol;
    // cin >> symbol;

    // bool isEngVowel = symbol == 'A' || symbol == 'E' || symbol == 'I' || symbol == 'O' || symbol == 'U' || symbol == 'a' || symbol == 'e' || symbol == 'i' || symbol == 'o' || symbol == 'u';

    // cout << isEngVowel;

    // --- Ex 4 ---

    // int grade;
    // cin >> grade;

    // bool isValidGrade = grade >= 2 && grade <= 6;

    // cout << isValidGrade;

    // --- Ex 5 ---

    // int num;
    // cin >> num;

    // cout << num / 10;

    // --- Ex 6 ---

    // double a, b;
    // cin >> a >> b;

    // double result = (a + b) * (a + b) * (a + b) * (a + b) - (a - b) * (a - b);
    // cout << result;

    // --- Ex 7 ---

    // int money;
    // cin >> money;

    // cout << money / 100 << endl;
    // money = money - 100 * (money / 100);

    // cout << money / 50 << endl;
    // money = money - 50 * (money / 50);

    // cout << money / 20 << endl;
    // money = money - 20 * (money / 20);

    // cout << money / 10 << endl;
    // money = money - 10 * (money / 10);

    // cout << money / 5 << endl;
    // money = money - 5 * (money / 5);

    // cout << money / 2 << endl;
    // money = money - 2 * (money / 2);

    // cout << money;

    // --- Ex 8 ---

    // int n1, n2;
    // cin >> n1 >> n2;

    // bool n1IsBigger = n1 > n2;
    // cout << n1IsBigger * n1 + !n1IsBigger * n2;
}